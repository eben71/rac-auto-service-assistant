# Visual check — Task 002

The images in `screenshots/` were captured from the production build in headless Chrome at 1208 × 860 desktop and 390 × 844 mobile. The journey used only synthetic demonstration details (`DEMO16` and `demo@example.invalid`). The browser check reported no horizontal overflow at either width.

Compared with all five supplied images in `docs/reference/screenshots/`, the refined UI follows the sidebar/content split, content offset, heading and field positions, yellow actions, stepper, vehicle panel, service-card sequence and additional-service checklist. The assistant panel intentionally appears above the manual service cards, shifting their vertical position. Exact RAC branding and font matching remain pending approved standalone assets. Service descriptions remain synthetic fixture copy and do not claim real package inclusions or prices.
